# NDKTU Monitoring Platform — nginx
# Serves the Vite frontend AND proxies the API to the backend on one domain.
# Install: copy to /etc/nginx/sites-available/ and symlink into sites-enabled.

# WebSocket upgrade map: "upgrade" only for real WebSocket requests.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      '';
}

server {
    listen 80;
    listen [::]:80;
    server_name face.api.nsumt.uz;

    client_max_body_size 50M;          # allow image uploads

    # ---- Hide Swagger / API docs (must come before /api/) ---------------
    location = /api/docs         { return 404; }
    location = /api/redoc        { return 404; }
    location = /api/openapi.json { return 404; }

    # ---- API: /api/* -> backend, with the /api prefix stripped ----------
    # FastAPI routers have no /api prefix (/cameras, /users, ...), and the
    # /uploads static mount is reached via /api/uploads/*.
    location /api/ {
        rewrite ^/api/(.*)$ /$1 break;
        proxy_pass http://127.0.0.1:8000;

        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade    $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        proxy_read_timeout 3600s;      # long-lived camera streams
        proxy_send_timeout 3600s;
    }

    # ---- Frontend: everything else -> Vite dev server -------------------
    location / {
        proxy_pass http://127.0.0.1:5173;

        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade    $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}
